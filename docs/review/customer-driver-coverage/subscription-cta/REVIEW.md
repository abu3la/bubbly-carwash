# Subscription-first customer CTA, 2026-09-05

For customers without a booking or subscription, home presents monthly subscriptions immediately after the greeting. Its price comes from the live catalogue's lowest monthly plan, with a 30-day cycle label. The primary action is اشترك الآن / Subscribe now and opens /club. Active subscribers see their upcoming or current wash first, followed by a quieter summary of their actual plan and إدارة اشتراكي / Manage my subscription, opening /club/dashboard. One-off-wash promotional controls were removed from home; the existing booking implementation remains available without changes to billing or backend rules.

Profile, upcoming-bookings empty state and the final introduction now use subscription-focused actions. Filtered dates and active/past empty tabs show contextual messages instead of advertising another purchase. Subscription intent is forwarded through phone and OTP so a fully configured customer lands in package selection after signing in. Completing new vehicle onboarding or browsing past the location step opens monthly packages; account-maintenance return routes remain unchanged. Package-selection buttons explicitly say Subscribe to the named package and open the recurring schedule.

Validation:
- Client TypeScript and full client ESLint passed; git diff whitespace check passed.
- Actual iOS Simulator pointer checks with isolated synthetic API fixtures: home → Subscribe now → monthly plans → Subscribe to Basic → recurring schedule; profile → subscribe → plans; intro pages → Start your subscription → phone/OTP → plans; active subscriber home → Manage my subscription → membership dashboard → schedule; empty upcoming bookings show membership management for subscribers and Subscribe now for nonmembers, and the latter opens plans.
- Captured the revised home and monthly packages in this directory. Reviewed Arabic hierarchy, full button labels, contrast, gutters, centering and full first-screen visibility. The approved palette and Plex type remain intact. Controls use real app routes, and no content is hidden behind an entrance animation.
- No real SMS, payment, subscription purchase, database migration or app deployment was performed. Screenshot fixtures use synthetic customer/villa/price records, and do not establish real coverage or a purchased subscription.

## Next-wash home follow-up, 2026-09-05

The closest paid scheduled booking is now the leading home ticket, with Riyadh Today/Tomorrow (or weekday), Gregorian date including year, explicit AM/PM time, service, vehicle and address. Server-driven active work takes precedence and stays visible until completed even if its time window elapsed. This fixes the API's descending scheduled_at order showing the farthest future booking. Home updates its clock each minute while focused and refreshes customer data on focus/foreground.

The ticket and View wash details open the booking's date and upcoming/active tab. A subscriber without appointments sees Schedule a wash, which resets the draft, selects the plan's included service and subscription payment source, then opens real vehicle/location selection. Loading and fetch failure remain distinct from an empty schedule; retry recovers the display. No appointment is invented in the product.

Validation: client TypeScript and full ESLint passed. Six pure selection/date tests passed for reverse ordering, exclusion of unpaid/closed/expired/invalid bookings, active work, Riyadh midnight, AM/PM and Gregorian year boundaries. Native pointer checks passed for upcoming details (6 September, 08:00), active details (5 September, active tab), empty-state scheduling, error and retry recovery. The screenshot is from the actual iOS Simulator with synthetic local data, not a real purchase.

Design-law recheck: reviewed the new hierarchy and retained the approved Bubbles ticket silhouette, palette, Plex type and primitives. The date/time and ticket stub remain centered/aligned and clear of both notches; text and controls have full gutters and readable contrast. No new decorative gradients, glows, icon containers, entrance hiding, fabricated metrics or unrelated marketing sections were added. Button labels remain whole, controls have real destinations, and subscription management is visually secondary to the upcoming wash. Existing scroll content remains reachable below the tab bar. Only the screenshot gallery is published; mobile/API production deployment remains unchanged.

## Designed subscription summary and saved days, 2026-09-05

A shared MembershipSummary now presents the actual plan name, a grammatically correct weekly wash count and the chosen weekday/time pairs as an evenly aligned schedule inside one tonal card. Home keeps the next wash above it, removes the member's marketing tagline and tightens the appointment spacing so both sections fit in the reviewed first viewport. The same summary appears in subscription management. Manage my subscription remains a real navigation action; the displayed day/time cells are read-only, grouped accessibly, and do not pretend to edit appointments.

GET /memberships/current now adds weeklySchedule from membership_signup_slots for the authenticated current membership. Weekly occurrences dedupe to Riyadh weekday/time pairs. Cancelled individual appointments do not rewrite the original selected rhythm. Existing subscribers without saved signup rows receive an empty array, and old deployed APIs without the field show a truthful unavailable state instead of fabricated days. No database migration is required.

Validation: client and API TypeScript passed; full client and focused API ESLint passed. Seven new helper/route tests cover timezone conversion, sorting and deduplication, empty/malformed rows, authenticated ownership, no membership and unauthenticated access. Native Simulator checks covered Arabic two- and three-day plans, English three-day alignment (including Wednesday), and home to subscription management. Final Arabic home and subscription screenshots plus an English three-day review capture are in this directory. Narrow effective width (viewport divided by font scale) switches to aligned full-width weekday/time rows, with shrinkable weekday text for large type; those breakpoints were code-reviewed, not device-matrix tested.

Final design-law audit: retained the approved Bubbles palette and Plex fonts; used the existing ticket as the home signature, a bare calendar mark and a restrained violet-tint schedule card. No new shadows, decorative animation, gradients, metadata chips, icon tiles, fake numbers or dead actions. Verified full day names and times, equal column baselines, RTL/LTR order, contrast, gutters, centered cells and clearance above the tab bar in the captured first viewport. Nonmember subscription CTA is unchanged. Only the private screenshot gallery is published; mobile/API changes remain local and screenshots contain synthetic test fixtures.

## Subscription tone correction, 2026-09-05

Removed the entire Subscription rule panel and its missed-appointment warning from subscription management in both languages. The existing cycle date now sits quietly inside the plan card as ساري حتى / Active until. Removed the repeated forfeiture paragraph from package browsing and the no-credit/no-rollover emphasis from purchase review; the review instead states the weekly count, 30-day duration and selected recurring schedule. Prices, consent and backend behavior are unchanged.

Client TypeScript, full client ESLint and whitespace checks passed. Native Simulator review confirmed the rule panel is absent, the date remains legible inside the plan card, and the management-to-schedule controls remain visible. Updated the actual subscription screenshot and gallery ZIP using synthetic local fixtures. This is a local app copy/layout change; the screenshot gallery alone is published.
